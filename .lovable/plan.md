

## Make Auth Page Left Square More Visible

Update `src/pages/Auth.tsx` line 228: increase the left square's border opacity from `border-primary-glow/45` to `border-primary-glow/60` so it stands out more against the background.

